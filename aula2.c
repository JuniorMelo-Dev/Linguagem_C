#include <stdio.h>//biblioteca para usar os métodos de entrada e saída de dados, tipo = "printf"

int main() {

    //variáveis => tipo nomeVariável = valorVariável
    int valor = 0;

    valor = 10;
    printf("%d", valor);

    

    return 0;
}